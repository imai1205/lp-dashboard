# Turso (libSQL) ──────────────────────────────────────────────
# 本番:  turso db show <db-name> --url  /  turso db tokens create <db-name>
# ローカル開発: SQLiteファイルを指定するだけ (auth tokenは不要)
TURSO_DATABASE_URL="file:./local.db"
TURSO_AUTH_TOKEN=""
